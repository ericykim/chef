import React from 'react';
import { Input } from 'antd';
import styles from './styles.css';

const CreateRecipe = (props) => {
  return (
    <div data-testid={'CreateRecipe'}>
      <Input />
    </div>
  );
};

export default CreateRecipe;
